# 🧠 对话功能 · 使用与开发说明

> 本文档面向两类读者：
> - **使用者**：怎么看懂对话功能、每一步回复是怎么来的、为什么这样解释、如何配置与提速。
> - **开发者**：对话问答逻辑与回复逻辑的完整技术路线（数据流 / 模型路由 / 板块识别 / 图推画图 / 扩展点），方便继续开发。
>
> 配套：`README.md`（项目总览）、`使用说明.md`（全功能用户手册）、`PROJECT_ROADMAP.md`（路线图）、`CONVENTIONS.md`（开发规范）。

---

## 一、对话功能是什么

对话页（💬）是行测助教的核心入口：把题目（文字或截图）发进来，AI 按「名师方法论」解题讲透，并支持错题入库、变式题、复盘、朗读等。核心闭环：
**发题 → 板块识别 → 注入专项名师方法 → 模型作答（流式） → 复盘指引 → 存错题/变式/追问**。

---

## 二、使用者：怎么用（快速上手）

### 2.0 使用前：先登录（本地账号）
应用默认开启「本地登录门」，**首次打开先注册（用户名 2-20 位 + 密码 ≥4 位）→ 注册即自动登录**；之后勾「记住我」7 天内免登录。纯本地账号：用户名 + 密码，只存本机 localStorage（无服务器、不上传、无第三方服务）；忘记密码可在登录页点「重置本地账号」，或在设置 → 🔐 账号与安全 中关闭登录门。详见《使用说明.md》「🔐 注册与登录」。
### 2.1 发题方式
| 方式 | 操作 | 适合 |
|---|---|---|
| 纯文字题 | 直接输入题干/选项 | 言语、判断、数量、资料、常识等文字题 |
| 发图截图 | 点 📷 选图，或粘贴截图 | 图推/几何/表格截图、文字截图 |
| 语音 | 点 🎤 | 不方便打字时 |
| 上传链接/文件 | 点 🔗 | 需要联网或导入材料 |

### 2.2 发送前选「作答模式」（重要）
输入栏有 **`🧠 深度 / ⚡ 快答`** 切换（默认 🧠 深度，选择会记住）：
- **🧠 深度**：用思考模型（deepseek-v4-flash）完整推理 → **难题、文字截图题、图形题用这个，质量最稳**（较慢，可能 1 分钟左右）。
- **⚡ 快答**：用「对话快模型」（设置里填 deepseek-chat 等非思考模型）秒回 → 简单题、熟练题刷起来快（约 10 多秒，无思考过程）。

### 2.3 配置要求（必须先在 ⚙️ 设置里配好）
| 配置 | 位置 | 作用 |
|---|---|---|
| 文字模型 + Key | 设置→模型与语音 | 所有文字作答（DeepSeek 推荐） |
| 视觉模型 + Key | 设置→视觉模型 | 看图/公式/图表（DeepSeek deepseek-v4-flash-vision-exp 或智谱/通义） |
| 🚀 对话快模型 | 设置→模型与语音 | 快答模式用（填 deepseek-chat 等非思考模型名，同 Key） |
| 🖼 图形增强（可选） | 设置→图形增强 | ①图片含图形时自动复刻+标注 ②快模型不识图时读图兜底；可用硅基流动免费额度/Ollama 本地 |

### 2.4 回复框里的附加功能
- **💭 思考过程（可折叠）**：深度模式下模型的推理过程，可点开看「为什么这么想」。
- **📐 原图标注（图推）**：文字图推题，若回复没画图，会自动补画一张「重绘原图 + 标注（辅助线/箭头/高亮/对称轴）」帮助看懂规律。
- **🖼 AI 图形复刻**：发**含图形/表格的图**时，自动用图形增强模型复刻原图 + 标注规律线索 + 一句话「📐 规律」；**纯文字截图不会触发**（省 token），需要可手动点「🖼 图形增强」。
- **📌 存错题 / 🔁 变式题 / 📄 复盘 / 💬 追问 / 🔊 朗读**：每条回复都有。

---

## 三、回复逻辑：一道题是怎么被解释的（为什么这样讲）

发送后按以下流水线处理（这也是开发者要理解的核心链路）：

```
用户输入(文字/图片)
   ↓ ① 组装消息（文字 + 图片，图片压缩）
   ↓ ② 板块识别 detectBanKuai() → 判断是 言语/判断/图推/资料/数量/常识/政治/…
   ↓ ③ 注入对应「名师专项提示词」KB[板块]（薛睿24诀/郭熙×张弓/小P/小黑…）
   ↓ ④ 智能识图路由（仅图片）：
   │    · 主视觉模型可识图 → 图片直接给视觉模型看
   │    · 主视觉不能识图 + 配了图形增强 → 用图形增强读图（区分纯文字/含图形）→ 文字模型作答
   │    · 都没有 → 仍接收图片，注入系统提示礼貌引导描述（不拒收、不假装看到）
   ↓ ⑤ 模型选择：🧠深度=思考模型 / ⚡快答=快模型（图片+快模型不识图→先读图再答）
   ↓ ⑥ 组装上下文：系统提示词(通用SYS + 板块专项) + 最近对话历史(≤20条/裁剪)
   ↓ ⑦ 流式输出 chatStream（思考过程折叠 + 正文逐字）→ 用户实时看到
   ↓ ⑧ 高效复盘指引 buildReview()：题型/错因/秒杀/巩固动作（模型没给就本地按板块兜底）
   ↓ ⑨ 附加：图推回复无图→补画标注图；图片含图形→图形增强复刻+标注；自动朗读
```

### 关键点：为什么「具体问题会这样解释」
1. **先定板块再讲方法**：同样一句话，言语题走郭熙行文脉络、逻辑题走薛睿五步+13美丑、图推走 24 诀/6眼破题法、资料走小P四大神器——**先识别板块，再注入对应名师的解题框架**，所以解释有"名师味"。
2. **题型判定→名师分步→选项逐项→陷阱+秒杀→复盘**：系统提示词强制这个输出结构，保证每道题都按此讲透。
3. **图推必画图**：图推专项提示词要求「重绘原图 + 标注」，前端把 ```svg 自动渲染（零裁切修复），必要时补画，让规律"一眼可见"。
4. **图片题智能分流**：纯文字截图 → 走文字模型；含图形 → 视觉模型直看或图形增强读图，各取所长，不浪费 token。

---

## 四、开发者：技术路线与关键代码

### 4.1 涉及文件
| 文件 | 职责 |
|---|---|
| `01_源码/src/components/ChatPage.vue` | 对话 UI + 发送/回复主流程（send → runChat） |
| `src/api/client.js` | 底层模型调用：`chatStream`（流式）/`chatOnce`（单次）、`activeCfg`（双模型路由）、`supportsVision`（可识图判断）、`imgRoute`（智能识图路由决策）、`readQuestionFromImage` 接入 |
| `src/api/figEnhance.js` | 图形增强：`figCfg`（是否配置）、`analyzeFigImage`（复刻+标注）、`readQuestionFromImage`（读题提取，type=text/graph） |
| `src/api/detect.js` | 板块识别 `detectBanKuai`（关键词→板块） |
| `src/api/sys.js` | 系统提示词组装 `buildSys`（SYS + KB 专项） |
| `src/kb.js` | 知识库：通用 `SYS` + 各板块专项 `KB`（含图推画图铁律） |
| `src/utils/review.js` | 高效复盘指引 `buildReview`（本地兜底） |
| `src/utils/renderMd.js` + `svgFix.js` | Markdown/SVG 渲染（补 viewBox、白图描边，零裁切） |
| `src/utils/auth.js` | 本地账号登录门：注册/登录/改密/删号/重置/开关（SHA-256 加盐，`xc_auth`） |
| `src/store.js` | 全局状态（对话/错题/配置，localStorage 持久化；`uiCtx.panel` 供萌宠感知当前界面） |
| `src/utils/petKnowledge.js` | 萌宠功能百科（30 条功能）+ 界面感知 + 资料速算双师卡索引（v3.7.24） |
| `src/utils/dataTrainLib.js` | 资料速算「理论课堂」双师 52 张知识点卡 + 查询/映射（v3.7.22） |
| `src/utils/quizVerify.js` / `quizVerifyProfiles.js` / `quizLog.js` | 出题本地质检（严格单选）+ 板块子命题人档案 + 出题历史反哺（v3.7.17-18） |
| `src/utils/costTrack.js` | AI 用量与花费实时追踪（官方计价表） |

### 4.2 核心数据流（代码级）
1. `send()`：校验 → 取模型 → 智能识图路由（必要时先 `readQuestionFromImage` 读图）→ 生成用户消息 `userMsg`（含 `_curImgRead`/`_imgType`）→ 入 `store.msgs` → `await runChat()`。
2. `runChat()`：
   - 读取最后一条用户消息（文字/图片/已读图内容）。
   - 计算 `replyC`（模型）：🧠深度=主模型；⚡快答=快模型（`xc_chat_fast_model`/`xc_fast_gen_model`）；图片+快模型不识图→先 `readQuestionFromImage` 读图。
   - 组装 `sys`（通用 + 板块专项 + 快答节奏/图片情况说明）。
   - 组装 `history`（最近 20 条，图片按视觉能力保留，`_curImgRead` 优先）。
   - `chatStream(msgs, replyC, onDelta)` 流式输出 → 落 `store.msgs`。
   - 成功后：`buildReview` 复盘 → 图推无图则 `drawTutuAnno` 补画 → 图片含图形则 `maybeFigEnhance` 复刻。
3. **模型选择优先级**：
   - 文字：🧠深度=文字模型；⚡快答=对话快模型（若配）。
   - 图片：主视觉可识图→直发；否则图形增强读图→文字/快模型；再无→graceful 引导。
4. **板块识别**：`detectBanKuai(text)` 返回板块 → `PLATE_MODE[板块]` → `buildSys(mode)` 注入 `KB[mode]`。

### 4.3 关键设计决策（为什么这么做）
- **双模型路由（文字/视觉分离）**：文字走便宜快模型，图片走视觉模型，互不干扰，控制成本与质量。
- **智能识图路由 `imgRoute`**：vision 直发 / fig-read 读图 / graceful 三档——图片永不拒收、不假装看到、不浪费（纯文字截图不走视觉/复刻）。
- **深度/快答按题切换**：质量与速度不能兼得，让用户按题选；快模型只作用于快答模式。
- **图推画图铁律 + 补画 + 复刻标注**：三层保障图推解析"有图有真相"——提示词要求画、回复没画就补画、图片就复刻标注。
- **SVG 零裁切（svgFix）**：AI 出的 SVG 统一补 viewBox/内容自适应/白图描边，任何屏幕宽度都不裁切、不空白。
- **萌宠全知底座（petKnowledge + petPersona）**：公共底座（功能百科+界面感知+名师KB+双师卡+记忆库+统计）与角色性格层分离——四角色（薛神/章若楠/李星云/姬如雪）口吻不同但知识能力一致，任何功能/板块问题都能答。
- **出题质检三重保障（quizVerify + quizVerifyProfiles + quizLog）**：本地硬规则（唯一单选）→ 板块语义校验（子命题人）→ 历史失败反哺提示词，不合格必重出，质量持续提升。
- **本地零额度生成器（dataTrainGen/dataTrainLib）**：宽范围确定性随机 + 唯一解程序校验，训练不耗 API；理论课堂 52 卡为静态提炼数据，离线可用。

### 4.4 扩展点（想做新功能从哪改）
- 新增板块/名师方法：改 `kb.js` 的 `KB[板块]` + `detect.js` 关键词 + `PLATE_MODE`。
- 新增模型服务商：改 `api/client.js` 的 `supportsVision`/`hds` + 设置页预设。
- 调整回复结构：改 `kb.js` 的 `SYS` 输出协议 + `review.js` 复盘。
- 调整图推画图策略：改 `kb.js` 图推画图铁律 + `ChatPage.drawTutuAnno`/`maybeFigEnhance`。
- 对话数据迁移/持久化：`store.js` + `localStorage` key（`xc_*`）。

---

## 五、常见问题（FAQ）

| 问题 | 原因/解决 |
|---|---|
| 回复要思考很久？ | 深度模式用思考模型（deepseek-v4-flash）属正常；切「⚡快答」或用快模型秒回。 |
| 发图说"看不到图片"？ | 主视觉未配或模型不识图：配「视觉模型」（DeepSeek vision/智谱/通义）或「图形增强」自动读图。 |
| 纯文字截图也触发图形增强？ | 已修复：仅图片含图形/表格（或文字带图推关键词）才自动复刻；否则不触发、可手动。 |
| 图推回复没画图？ | 会自动「补画标注图」；建议在出卷参数填「出题快模型」让补画更快。 |
| 打开就要登录，忘记密码了？ | 本地账号只存本机：登录页「🔄 重置本地账号」清空后重新注册（学习数据不受影响）；或设置里关闭登录门。 |
| 登录方式？ | 纯本地账号：用户名 + 密码（设置里可关闭登录门；忘记密码可「重置本地账号」重新注册）。 |
| 图形/公式不显示或裁切？ | svgFix 已兜底（补 viewBox/白图描边），任意宽度零裁切；仍异常可反馈。 |

> ⚠️ 本项目仅供个人学习使用，切勿商用；API Key 只存本机浏览器 localStorage，应用无后端不上传。

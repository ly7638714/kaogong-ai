# 行测 AI 智能助教 · 开发说明（Developer Guide）

> 适用对象：后续参与本项目的开发者 / 维护者。本文档与「用户使用说明」互补，只讲**工程结构与开发约定**。
> 最后更新：v3.7.1（设置面板 6 组重构后）

---

## 1. 项目概览

- **定位**：个人备考自用工具 —— 把公考行测名师方法论「蒸馏」成 AI 可执行的答题协议，覆盖 看板 / 对话 / 知识库 / 积累 / 统计 / 错题 / 3D 数据 等板块。
- **技术栈**：Vue 3（Composition API + `<script setup>`）+ Vite + PWA（Workbox 离线缓存）+ 原生 JS（无重型框架）。
- **无后端**：所有数据只存浏览器 localStorage；AI 能力全部走用户自填的第三方 API（DeepSeek / 智谱 / OpenAI / 通义 / 本地 Ollama 等）。
- **目录约定**：`01_源码` 是**唯一活跃源码**；`02_发布物`（网页发布包 + zip）、`04_安卓\行测AI助手`（HBuilderX web 资源）均由构建产物同步，**不要手改**。

---

## 2. 目录结构

```
kaogong-review-skill-main/
├─ 01_源码/                  # 唯一活跃源码（开发/构建都在这里）
│  ├─ src/
│  │  ├─ App.vue             # 全局壳：顶栏/导航/设置面板/萌宠浮窗/各弹窗
│  │  ├─ main.js             # 入口：store 加载 → createApp → 错误处理 → mount
│  │  ├─ store.js            # 全局状态：store.cfg / msgs / wqs / myMem / notes
│  │  ├─ kb.js               # 知识库：名师方法论提示词（按板块/模式）
│  │  ├─ styles.css          # 全局样式（含设置面板、萌宠、语音市场等）
│  │  ├─ api/                # AI 适配层（文字/视觉/图形增强/测试连接）
│  │  ├─ components/         # 各板块页面（Cockpit/Chat/Kb/Stats/Wrong/3D…）
│  │  ├─ utils/              # 功能模块（tts 语音/pet 萌宠/music 音乐/export 导出…）
│  │  └─ __tests__/          # Vitest 单元测试（15 个文件 / 177 用例）
│  ├─ index.html
│  └─ package.json
├─ 02_发布物/                # 构建产物 + 发布 zip（自动同步）
├─ 04_安卓/行测AI助手/       # 安卓 web 资源（自动同步）
└─ scripts/sync-dist.ps1     # 一键构建并同步三端
```

---

## 3. 模块地图（核心）

| 模块 | 文件 | 职责 |
|---|---|---|
| 全局壳 / 设置面板 | `src/App.vue` | 顶栏、六大板块导航、设置弹窗（6 组手风琴）、萌宠浮窗、随手记、导出弹窗 |
| 全局状态 | `src/store.js` | `store.cfg` 设置、对话、错题、记忆库、笔记的读写与 localStorage 持久化 |
| 知识库 | `src/kb.js` | 名师方法论提示词（判断推理薛睿/言语三师/图推/资料/数量/常识/时政…） |
| AI 适配 | `src/api/*` | 文字/视觉/图形增强的厂商适配与连接测试 |
| 语音朗读 | `src/utils/tts*.js` | 四引擎（智谱 GLM-TTS / OpenAI 兼容 CosyVoice2 / Edge / 系统语音）、音色市场、克隆、无缝隙播放 |
| 萌宠系统 | `src/utils/pet.js` | 内置角色（薛神/章若楠/李星云/姬如雪，锁定）+ 自定义角色；形象/声线/人设 |
| 背景音乐 | `src/utils/music.js` | 学习曲目、在线/本地音频、音量/循环 |
| 导出 | `src/utils/export.js` | AI 整理导出 / 直接导出 Word/PDF/Markdown、错题导出 |
| WebDAV | `src/utils/webdav.js` | 云同步备份 |
| 学习统计 | `src/utils/study.js` | 学习时长/打卡追踪 |
| 组件页面 | `src/components/*` | CockpitPage 看板 / ChatPage 对话 / KbPage 知识库 / WrongPage 错题 / StatsPage 统计 / Data3DPage 3D 数据 / SolidTrain 立体图推 / DataTrain 资料速算（含理论课堂） |
| 资料速算生成器 | `src/utils/dataTrainGen.js` | 四层能力训练本地生成器（判题型/找数据/公式/速算），宽范围确定性随机 + 唯一解校验 + 三阶段速算（v3.7.19-21） |
| 双师知识库 | `src/utils/dataTrainLib.js` | LY×小P 52 张知识点卡（理论课堂），`searchCards/cardById/cardForQuiz`（v3.7.22） |
| 出题质检 | `src/utils/quizVerify*.js` + `quizLog.js` | 本地严格单选校验 + 板块子命题人档案 + 出题历史反哺（v3.7.17-18） |
| 萌宠知识库 | `src/utils/petKnowledge.js` | 30 条项目功能百科 + 界面感知 + 双师卡索引（v3.7.24） |
| 用量追踪 | `src/utils/costTrack.js` | AI 每次调用 token/花费实时记录 + 官方计价表 |
| SVG 修复 | `src/utils/svgFix.js` | AI SVG 补 viewBox/内容自适应/白图描边，零裁切 |

---

## 4. 设置面板结构（6 大分组）

设置弹窗是 `App.vue` 内最大的模板区块，按 **6 组手风琴** 组织：

| 组 key | 标题 | 内容区块（`sec-t`） |
|---|---|---|
| `ai` | 🧠 模型与 AI | 文字模型(set-api) / 视觉模型(set-vision) / 图形增强(set-fig) |
| `voice` | 🗣️ 语音朗读 | 语音总览(set-voice)（含朗读引擎 / 音色市场 / 克隆 / 本机语音） |
| `look` | 🎨 外观与主题 | 主题与外观(set-look) / 主题预设 / 随手记 / 备考冲刺 / 导出偏好 / 背景(set-bg) |
| `data` | 💾 数据与同步 | 数据保存位置(set-data) / 数据管理 / WebDAV / 时政范围 |
| `fun` | 🎵 趣味与陪伴 | 背景音乐 / 曲目列表 / 萌宠 |
| `help` | ❓ 帮助与关于 | 新手引导 / 使用帮助(set-help) / 关于·开发者说明(set-about) / 模型说明 / 错误日志 |

**关键代码点（App.vue）**：
- `SET_GROUP_META`：6 组导航元数据（`setNav` 直接引用它）。
- `SEC_GROUP`：每个锚点 `sec-t` 的 id → 所属组映射；`scrollSet(id)` 会自动展开对应组并滚动定位。
- `setGroup`：当前展开的组（默认 `'ai'`）；`toggleSetGroup(k)` 展开/收起。
- `stCfg`：顶部状态总览（文字/视觉是否配 Key、朗读引擎、萌宠角色、数据位置）。
- 区块标准：`<div id="set-xxx" class="sec-t">标题</div>` + 可选 `<div class="sec-desc">一行说明</div>` + 内容（`.fld` 表单 / `.guide` 折叠教程 / `.exp-choices` 按钮组）。

**新增一个设置项的标准流程**：
1. 在目标组内添加区块（`sec-t` + `sec-desc` + 表单控件）。
2. 在 `store.cfg`（store.js 默认值）增加字段，并用 `saveCfg()` 持久化。
3. 如需被导航/引导定位：把区块 id 加入 `SEC_GROUP`（和 `SET_GUIDE`，可选）。
4. 跑 `npm run lint && npm test && npm run build`，再 `scripts/sync-dist.ps1`。

---

## 5. 配置键速查（`store.cfg`）

| 字段 | 含义 |
|---|---|
| `text` / `vision` | 文字/视觉模型：`prov`（厂商）/`key`/`url`/`model` |
| `fig` | 图形理解增强（`on`/`prov`/`key`/`url`/`model`） |
| `ttsMode` | 朗读引擎（glm/openai/edge/sys） |
| `ttsGm` / `ttsOpenAI` / `ttsEdgeVoice` | 各引擎音色配置 |
| `ttsOn` / `ttsRate` / `ttsPitch` / `ttsScene` | 自动朗读 / 语速 / 音调 / 场景音色 |
| `petSkin` / `skinVoices` / `skinImgs` / `customSkins` | 萌宠当前角色 / 各角色绑定声线 / 自定义形象 / 自定义角色列表 |
| `voiceCustom` | 音色市场本地管理：`names`（改名映射）/ `hidden`（隐藏列表） |
| `musicOn` / `musicVol` / `musicLoop` / `musicList` | 背景音乐 |
| `szFrom` / `szTo` | 时政时间范围 |
| `dataDir` | 本地保存文件夹 |
| `sys` / `kb` / `strm` | 自定义 System Prompt / 知识库增强 / 流式输出 |

**萌宠内置角色（PET_SKINS，utils/pet.js）**：顺序 = 薛神(xueshen) / 章若楠(zhangruonan) / 李星云(lixingyun) / 姬如雪(jiruxue) / 自定义(custom)。前四个 `locked: true` 不可更改；每个含 `hair/coat/…` 头像参数、`voice`（克隆原声 ID 或默认音色）、`persona` 人设。**注意**：Vue 模板里引用 `petSkin` 时自动解包，**不要写 `petSkin.value`**（这是曾踩过的坑）。

---

## 6. localStorage 存储键速查

| 键 | 内容 |
|---|---|
| `xc_cfg` | 全部设置（store.cfg） |
| `xc_msgs` | 对话记录 |
| `xc_wqs` | 错题集 |
| `xc_my_mem` | 记忆库（常识/时政/成语/实词/笔记） |
| `xc_notes` | 笔记 |
| `xc_pet` | 萌宠养成数据（xp/饱食度/心情） |
| `xc_errlog` | 最近错误日志（最多 50 条） |
| `xc_chat_fast_model` | 对话快模型名（免思考秒回） |

---

## 7. 语音系统（TTS）要点

- 四引擎统一入口：`speakPro / stopSpeakPro / speakingPro`（utils/tts.js），外部不直接碰引擎细节。
- 智谱克隆：`cloneZhipuVoice`（utils/ttsEngine.js）——自动 ASR 转写参考音频后调 `voice/clone`，返回 `voice` ID。
- 音色市场显示名：`listGmVoices` 会优先用 `GLM_PRESET_VOICES` 映射，其次 `CLONE_VOICE_DISPLAY`（内置克隆音色中文名），再回落 `voice_name`。
- 智谱接口注意：删除音色 `POST /voice/delete` 的 `voice` 字段要传**音色 ID**（不是名称），并带 `request_id`；**没有改名接口**（应用内改名是本地 `voiceCustom.names` 映射）。
- 朗读体验：长文按句分块并行预取、顺序入队，Web Audio 无缝拼接；`symbolsToChinese` 做数学/箭头符号的中文化朗读。
- **v3.7.24 丝滑增强**：分块参数 `chunkSize 84 / firstChunkSize 26`（speakPro 传入 glm/openai）；gapless 播放器改「并行解码（`gapDecode`）+ 串行调度（`gapStart` 按 `_gap.nextAt` 时间轴）」——块到即解码、调度严格按序，解码失败跳过不中断；`slideSynthesize` 预取窗口 W=4。
- **引擎复用**：`gmCfg()` 语音 Key 为空时自动复用「图形增强」里的智谱 Key（`store.cfg.fig` 匹配 bigmodel/zhipu/glm），因此零语音配置也能走 GLM 真人音色。

---

## 8. 质量与发布流程（每次改动必做）

```powershell
cd 01_源码
npm run lint        # ESLint 零告警（--max-warnings=0）
npm test            # Vitest，现有 254 用例全绿（24 个测试文件）
npm run build       # Vite 构建 + PWA 生成
cd ..
powershell -File scripts/sync-dist.ps1   # 构建并同步 02_发布物 / 04_安卓 / zip
```

- 提交信息建议带版本号（如 `feat: v3.7.1 …`）。
- `01_源码/dist` 被 gitignore；同步由 sync-dist.ps1 完成。
- 安卓 APK 需在 HBuilderX 用 `04_安卓` 资源重新云打包，本仓库不存 APK。

---

## 9. AI 用量与花费追踪（costTrack）

- 模块：`src/utils/costTrack.js` —— 记录每次 AI 调用（文字/视觉/图形增强/朗读/克隆），按可配置计价表估算金额，持久化 `xc_cost`，顶栏「💰 今日 ¥x」徽标实时显示，点击打开明细面板（今日/本周/累计 + 按功能/按模型 + 最近 60 条记录 + 计价表编辑 + 清空）。
- 埋点位置：`src/api/client.js`（chatOnce/chatStream：接口返回 `usage` 时用精确 token，否则按文本估算）；`src/api/figEnhance.js`（图形增强，本地 Ollama/LM Studio/Jan 计 ¥0）；`src/utils/ttsEngine.js`（朗读按字数、克隆固定费）。
- 功能标注：`setCostCtx('pet'|'exam'|...)`（client.js 导出）可在发起请求前标注功能归属，下一条记录自动消费后清空；未标注时按「消息含图或 cfg 为视觉 → 视觉识别，否则 → 文字问答」推断。
- 计价表：`DEF_PRICES`（元/千 token）+ `ttsPer1k`（朗读元/千字，默认 0.002）+ `cloneFee`（克隆元/次，默认 0.05）；用户可在面板里改，存 `xc_cost_p`。
- 注意：金额为估算，仅供「心里有底」，最终以服务商账单为准；新增 AI 调用时记得埋点。

## 10. 常见开发注意事项

1. **模板里别写 `.value`**：Vue 模板对顶层 ref 自动解包；`petSkin.value.char` 会报错，应写 `petSkin.char`。
2. **CRLF 行尾**：本仓库源码是 CRLF，用脚本改文件时按 `\r\n` 切分行，避免整文件换行被破坏。
3. **设置面板改分组**：区块物理上必须位于目标组的 `set-group-bd` 容器内，且每个组的 div 配平；新增区块直接放进对应组即可。
4. **不要直接改 02_发布物 / 04_安卓**：它们由同步脚本覆盖。
5. **萌宠锁定角色**：`locked: true` 的角色禁止改形象/声线（`setPetImg`/`petBindCloneVoice` 会拒绝）。
6. **萌宠知识底座**：萌宠"懂功能"靠 `petKnowledge.js`（`petFeatureText`/`petDetectUi`/`DATA_TRAIN_INDEX`）全量注入 `petPersona()` 公共底座；新增功能时记得在 `APP_FEATURES` 登记，萌宠才会知道。
7. **资料速算/理论课堂**：本地生成器在 `dataTrainGen.js`（唯一解校验 + 宽范围随机），知识卡在 `dataTrainLib.js`（52 卡静态数据）；新增知识卡需同步 `cardForQuiz` 映射与 `DATA_TRAIN_INDEX`。
8. **出题质检**：新出题入口必须过 `quizVerify.js`（严格单选），不合格带 reason 重出；`quizVerifyProfiles.js` 是各板块子命题人档案，`quizLog.js` 记录历史反哺。
